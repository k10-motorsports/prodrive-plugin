{
  "name": "racecor-mock-telemetry",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mock RaceCor.io telemetry server — serves the same data the SimHub plugin produces, driven by wall-clock time in a continuous loop.",
  "scripts": {
    "dev": "node dev-server.mjs"
  }
}
