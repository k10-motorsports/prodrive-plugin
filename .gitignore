# ── OS ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Node / JS ───────────────────────────────────────────────────────
node_modules/
dist/
/*/dist
*.tgz

# ── .NET ────────────────────────────────────────────────────────────
**/bin/
**/obj/
*.exe

# ── Logs ────────────────────────────────────────────────────────────
*.log
logs/

# ── SimHub reference DLLs (plugin SDK — committed for CI builds)
# lib/simhub-refs/*.dll

# ── Test results ────────────────────────────────────────────────────
**/test-results/

# ── Plugin CI build output ────────────────────────────────────────
racecor-plugin/build/

# ── Installer build artifacts ──────────────────────────────────────
installer/output/
installer/staging/

# ── Electron-builder output (dist/ already covered, but be explicit)
racecor-overlay/dist/

# ── Claude / MCP config (may contain API tokens) ─────────────────
.claude/
.vercel
.env*.local
