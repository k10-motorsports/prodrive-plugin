#!/usr/bin/env bash
# DEPRECATED — version stamping is now handled entirely by CI.
# See .github/workflows/release.yml → stamp-version job.
echo "Version stamping is automated in CI. No manual action needed." >&2
exit 0
